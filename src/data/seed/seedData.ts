import { Product } from '../../types';
import { PRODUCTS } from '../products';

/**
 * Seed dataset for initial database populate, offline fallback, or development mode.
 */
export const SEED_PRODUCTS: Product[] = PRODUCTS;
